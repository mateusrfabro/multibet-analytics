# Handoff Gusta — 28/04/2026

Repo `multibet-analytics`, branch `main`, commit `35a0178`.

## 1. Substituir 5 arquivos no orquestrador

| Arquivo no repo | O que faz |
|---|---|
| `ec2_deploy/pipelines/pcr_pipeline.py` | Substitui o PCR antigo. Cron 03:30 BRT. |
| `ec2_deploy/pipelines/segmentacao_sa_diaria.py` | Substitui o segmentacao antigo. Cron 04:00 BRT. |
| `ec2_deploy/pipelines/segmentacao_sa_enriquecimento.py` | Novo. Vai junto. |
| `ec2_deploy/pipelines/segmentacao_sa_smartico.py` | Novo. Vai junto. |
| `ec2_deploy/run_segmentacao_sa.sh` | Substitui o run script. |
| `ec2_deploy/db/slack_uploader.py` | Novo. Distribuição via Slack (substituiu e-mail). |

## 2. Atualizar `requirements.txt` na EC2

Adicionar (já está em `ec2_deploy/requirements.txt`):
```
slack-sdk>=3.27
```

Rodar:
```bash
pip install -r requirements.txt
```

## 3. Adicionar 2 vars no `.env` da EC2

```
SLACK_BOT_TOKEN=xoxb-8609546431681-11018748003444-tTxNdg9d5K3pQzhcJcLEfEy1
SLACK_CHANNEL_ID=C0B0JNUR2F6
```

> Token vai ser regenerado depois — eu te aviso e te passo o novo.

## 4. Tirar do cron

- `push_pcr_to_smartico.py` — não roda mais (segmentacao_sa_diaria.py já faz o push).

## 5. Manter cron

- `pcr_pipeline.py` — 03:30 BRT (atualiza PCR de todos os jogadores)
- `segmentacao_sa_diaria.py` — 04:00 BRT (gera CSV + push Smartico + posta Slack)

## Validação local feita

- ✅ CSV: 57 colunas, mesma ordem da referência do Castrin.
- ✅ Smartico: 136.753 jogadores atualizados hoje, 0 falhas.
- ✅ Slack: mensagem + upload de arquivo testados, post oficial de 28/04 entregue ao canal `#segmentacao-diaria-sa`.
